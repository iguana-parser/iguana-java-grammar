// @generated by iguana. Do not edit manually.
// You may distribute this file under any license of your choice. It embeds
// fragments of iguana's GPL-licensed source, and iguana grants an additional
// permission (GPL v3 section 7) that waives the GPL for generated parsers.
// Full text: https://github.com/iguana-parser/iguana-rs/blob/main/iguana-compiler/LICENSE-GPL

pub mod grammar_data ; pub mod parser ; pub mod parse_tree ; pub mod scanner ; pub mod types ; use std :: error :: Error ; use std :: fmt :: { self , Display , Formatter } ; use std :: time :: Duration ; use iguana_runtime :: { arena :: Arena , input :: Input , parser :: { ParseResult , Parser } , } ; use parse_tree :: * ; use parser :: JavaParser ; # [derive (Debug)] pub struct ParseError { pub line : u32 , pub column : u32 , pub len : u32 , pub message : String , }
impl Display for ParseError { fn fmt (& self , f : & mut Formatter < '_ >) -> fmt :: Result { write ! (f , "Parse error at line {}, column {}: {}" , self . line , self . column , self . message) } }
impl Error for ParseError { } pub struct ParseSuccess < T > { pub tree : T , pub parse_duration : Duration , pub tree_construction_duration : Duration , 
// True if an ambiguity node was added during parsing. Since the ambiguous node may end up in
// a dead branch (not reachable from root), the final parse tree may not be ambiguous, but
// this flag is used as a hint for checking for ambiguous parse trees. If the value is false,
// the parse is not ambiguous. If it's true, the caller should walk the parse tree to
// determine if there is an ambiguity node reachable from root. See contains_ambiguity.
 pub ambiguity_node_added : bool , }
/// Parses `input` starting from `CompilationUnit`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_compilation_unit < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a CompilationUnit < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_COMPILATION_UNIT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_compilation_unit (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `OrdinaryCompilationUnit`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_ordinary_compilation_unit < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a OrdinaryCompilationUnit < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ORDINARY_COMPILATION_UNIT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_ordinary_compilation_unit (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ModularCompilationUnit`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_modular_compilation_unit < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ModularCompilationUnit < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_MODULAR_COMPILATION_UNIT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_modular_compilation_unit (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ModuleDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_module_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ModuleDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_MODULE_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_module_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ModuleDirective`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_module_directive < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ModuleDirective < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_MODULE_DIRECTIVE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_module_directive (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `RequiresModifier`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_requires_modifier < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a RequiresModifier < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_REQUIRES_MODIFIER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_requires_modifier (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `PackageDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_package_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a PackageDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_PACKAGE_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_package_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ImportDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_import_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ImportDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_IMPORT_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_import_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `TypeDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_type_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a TypeDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_TYPE_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_type_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ClassDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_class_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ClassDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CLASS_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_class_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `NormalClassDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_normal_class_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a NormalClassDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_NORMAL_CLASS_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_normal_class_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `EnumDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_enum_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a EnumDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ENUM_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_enum_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `InterfaceDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_interface_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a InterfaceDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_INTERFACE_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_interface_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `NormalInterfaceDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_normal_interface_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a NormalInterfaceDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_NORMAL_INTERFACE_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_normal_interface_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `AnnotationTypeDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_annotation_type_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a AnnotationTypeDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ANNOTATION_TYPE_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_annotation_type_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ClassBody`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_class_body < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ClassBody < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CLASS_BODY , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_class_body (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ClassBodyDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_class_body_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ClassBodyDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CLASS_BODY_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_class_body_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `MethodDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_method_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a MethodDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_METHOD_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_method_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `MethodDeclarator`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_method_declarator < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a MethodDeclarator < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_METHOD_DECLARATOR , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_method_declarator (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `FormalParameterList`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_formal_parameter_list < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a FormalParameterList < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_FORMAL_PARAMETER_LIST , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_formal_parameter_list (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `FormalParameter`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_formal_parameter < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a FormalParameter < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_FORMAL_PARAMETER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_formal_parameter (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `LastFormalParameter`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_last_formal_parameter < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a LastFormalParameter < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_LAST_FORMAL_PARAMETER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_last_formal_parameter (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ReceiverParameter`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_receiver_parameter < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ReceiverParameter < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_RECEIVER_PARAMETER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_receiver_parameter (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Result`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_result < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Result < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_RESULT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_result (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Throws`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_throws < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Throws < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_THROWS , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_throws (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `MethodBody`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_method_body < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a MethodBody < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_METHOD_BODY , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_method_body (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ConstructorDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_constructor_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ConstructorDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CONSTRUCTOR_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_constructor_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ConstructorBody`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_constructor_body < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ConstructorBody < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CONSTRUCTOR_BODY , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_constructor_body (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ExplicitConstructorInvocation`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_explicit_constructor_invocation < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ExplicitConstructorInvocation < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_EXPLICIT_CONSTRUCTOR_INVOCATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_explicit_constructor_invocation (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `TypeList`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_type_list < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a TypeList < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_TYPE_LIST , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_type_list (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ClassTypeList`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_class_type_list < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ClassTypeList < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CLASS_TYPE_LIST , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_class_type_list (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `TypeParameters`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_type_parameters < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a TypeParameters < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_TYPE_PARAMETERS , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_type_parameters (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `TypeParameter`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_type_parameter < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a TypeParameter < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_TYPE_PARAMETER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_type_parameter (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `TypeBound`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_type_bound < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a TypeBound < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_TYPE_BOUND , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_type_bound (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Initializer`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_initializer < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Initializer < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_INITIALIZER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_initializer (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `EnumBody`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_enum_body < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a EnumBody < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ENUM_BODY , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_enum_body (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `EnumConstant`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_enum_constant < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a EnumConstant < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ENUM_CONSTANT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_enum_constant (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Arguments`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_arguments < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Arguments < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ARGUMENTS , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_arguments (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `EnumBodyDeclarations`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_enum_body_declarations < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a EnumBodyDeclarations < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ENUM_BODY_DECLARATIONS , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_enum_body_declarations (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Block`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_block < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Block < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_BLOCK , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_block (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `BlockStatement`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_block_statement < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a BlockStatement < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_BLOCK_STATEMENT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_block_statement (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `LocalVariableDeclarationStatement`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_local_variable_declaration_statement < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a LocalVariableDeclarationStatement < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_LOCAL_VARIABLE_DECLARATION_STATEMENT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_local_variable_declaration_statement (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `LocalVariableType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_local_variable_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a LocalVariableType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_LOCAL_VARIABLE_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_local_variable_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `FieldDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_field_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a FieldDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_FIELD_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_field_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `VariableDeclarator`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_variable_declarator < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a VariableDeclarator < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_VARIABLE_DECLARATOR , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_variable_declarator (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `VariableDeclaratorId`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_variable_declarator_id < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a VariableDeclaratorId < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_VARIABLE_DECLARATOR_ID , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_variable_declarator_id (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `VariableInitializer`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_variable_initializer < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a VariableInitializer < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_VARIABLE_INITIALIZER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_variable_initializer (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ArrayInitializer`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_array_initializer < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ArrayInitializer < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ARRAY_INITIALIZER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_array_initializer (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Annotation`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_annotation < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Annotation < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ANNOTATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_annotation (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Values`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_values < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Values < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_VALUES , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_values (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `AnnotationElement`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_annotation_element < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a AnnotationElement < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ANNOTATION_ELEMENT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_annotation_element (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ElementValuePair`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_element_value_pair < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ElementValuePair < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ELEMENT_VALUE_PAIR , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_element_value_pair (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ElementValue`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_element_value < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ElementValue < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ELEMENT_VALUE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_element_value (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ElementValueArrayInitializer`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_element_value_array_initializer < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ElementValueArrayInitializer < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ELEMENT_VALUE_ARRAY_INITIALIZER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_element_value_array_initializer (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `InterfaceBody`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_interface_body < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a InterfaceBody < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_INTERFACE_BODY , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_interface_body (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `InterfaceMemberDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_interface_member_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a InterfaceMemberDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_INTERFACE_MEMBER_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_interface_member_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ConstantDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_constant_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ConstantDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CONSTANT_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_constant_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `InterfaceMethodDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_interface_method_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a InterfaceMethodDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_INTERFACE_METHOD_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_interface_method_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `AnnotationTypeBody`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_annotation_type_body < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a AnnotationTypeBody < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ANNOTATION_TYPE_BODY , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_annotation_type_body (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `AnnotationTypeElementDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_annotation_type_element_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a AnnotationTypeElementDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ANNOTATION_TYPE_ELEMENT_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_annotation_type_element_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `AnnotationMethodDeclaration`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_annotation_method_declaration < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a AnnotationMethodDeclaration < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ANNOTATION_METHOD_DECLARATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_annotation_method_declaration (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `DefaultValue`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_default_value < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a DefaultValue < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_DEFAULT_VALUE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_default_value (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ClassModifier`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_class_modifier < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ClassModifier < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CLASS_MODIFIER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_class_modifier (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ConstructorModifier`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_constructor_modifier < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ConstructorModifier < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CONSTRUCTOR_MODIFIER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_constructor_modifier (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `InterfaceModifier`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_interface_modifier < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a InterfaceModifier < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_INTERFACE_MODIFIER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_interface_modifier (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ConstantModifier`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_constant_modifier < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ConstantModifier < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CONSTANT_MODIFIER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_constant_modifier (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `InterfaceMethodModifier`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_interface_method_modifier < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a InterfaceMethodModifier < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_INTERFACE_METHOD_MODIFIER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_interface_method_modifier (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `FieldModifier`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_field_modifier < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a FieldModifier < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_FIELD_MODIFIER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_field_modifier (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `MethodModifier`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_method_modifier < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a MethodModifier < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_METHOD_MODIFIER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_method_modifier (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `VariableModifier`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_variable_modifier < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a VariableModifier < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_VARIABLE_MODIFIER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_variable_modifier (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Statement`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_statement < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Statement < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_STATEMENT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_statement (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `StatementExpression`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_statement_expression < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a StatementExpression < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_STATEMENT_EXPRESSION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_statement_expression (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `SwitchBlockStatementGroup`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_switch_block_statement_group < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a SwitchBlockStatementGroup < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_SWITCH_BLOCK_STATEMENT_GROUP , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_switch_block_statement_group (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `SwitchLabel`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_switch_label < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a SwitchLabel < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_SWITCH_LABEL , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_switch_label (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ForControl`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_for_control < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ForControl < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_FOR_CONTROL , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_for_control (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `CatchClause`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_catch_clause < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a CatchClause < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CATCH_CLAUSE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_catch_clause (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `CatchType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_catch_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a CatchType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CATCH_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_catch_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Finally`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_finally < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Finally < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_FINALLY , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_finally (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ResourceSpecification`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_resource_specification < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ResourceSpecification < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_RESOURCE_SPECIFICATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_resource_specification (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Resource`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_resource < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Resource < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_RESOURCE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_resource (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ForInit`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_for_init < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ForInit < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_FOR_INIT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_for_init (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ForUpdate`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_for_update < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ForUpdate < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_FOR_UPDATE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_for_update (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Expression`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_expression < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Expression < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_EXPRESSION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_expression (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Primary`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_primary < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Primary < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_PRIMARY , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_primary (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `QualifiedIdentifier`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_qualified_identifier < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a QualifiedIdentifier < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_QUALIFIED_IDENTIFIER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_qualified_identifier (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `SuperSuffix`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_super_suffix < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a SuperSuffix < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_SUPER_SUFFIX , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_super_suffix (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `NonWildTypeArguments`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_non_wild_type_arguments < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a NonWildTypeArguments < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_NON_WILD_TYPE_ARGUMENTS , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_non_wild_type_arguments (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Type`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Type < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `PrimitiveType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_primitive_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a PrimitiveType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_PRIMITIVE_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_primitive_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ReferenceType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_reference_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ReferenceType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_REFERENCE_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_reference_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ClassType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_class_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ClassType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CLASS_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_class_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ClassTypeToInstantiate`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_class_type_to_instantiate < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ClassTypeToInstantiate < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CLASS_TYPE_TO_INSTANTIATE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_class_type_to_instantiate (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `TypeArguments`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_type_arguments < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a TypeArguments < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_TYPE_ARGUMENTS , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_type_arguments (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `TypeArgument`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_type_argument < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a TypeArgument < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_TYPE_ARGUMENT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_type_argument (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ArrayType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_array_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ArrayType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ARRAY_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_array_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `UnannType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_unann_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a UnannType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_UNANN_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_unann_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `UnannPrimitiveType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_unann_primitive_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a UnannPrimitiveType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_UNANN_PRIMITIVE_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_unann_primitive_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `UnannReferenceType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_unann_reference_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a UnannReferenceType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_UNANN_REFERENCE_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_unann_reference_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `UnannClassType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_unann_class_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a UnannClassType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_UNANN_CLASS_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_unann_class_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `UnannArrayType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_unann_array_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a UnannArrayType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_UNANN_ARRAY_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_unann_array_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Literal`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_literal < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Literal < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_LITERAL , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_literal (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Selector`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_selector < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Selector < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_SELECTOR , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_selector (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `MethodInvocation`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_method_invocation < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a MethodInvocation < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_METHOD_INVOCATION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_method_invocation (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `TypeArgumentsOrDiamond`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_type_arguments_or_diamond < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a TypeArgumentsOrDiamond < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_TYPE_ARGUMENTS_OR_DIAMOND , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_type_arguments_or_diamond (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ClassInstanceCreationExpression`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_class_instance_creation_expression < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ClassInstanceCreationExpression < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_CLASS_INSTANCE_CREATION_EXPRESSION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_class_instance_creation_expression (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `ArrayCreationExpression`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_array_creation_expression < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a ArrayCreationExpression < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_ARRAY_CREATION_EXPRESSION , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_array_creation_expression (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Lambda`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_lambda < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a Lambda < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_LAMBDA , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_lambda (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `LambdaParameters`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_lambda_parameters < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a LambdaParameters < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_LAMBDA_PARAMETERS , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_lambda_parameters (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `LambdaParameterList`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_lambda_parameter_list < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a LambdaParameterList < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_LAMBDA_PARAMETER_LIST , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_lambda_parameter_list (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `LambdaParameter`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_lambda_parameter < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a LambdaParameter < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_LAMBDA_PARAMETER , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_lambda_parameter (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `LambdaParameterType`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_lambda_parameter_type < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a LambdaParameterType < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_LAMBDA_PARAMETER_TYPE , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_lambda_parameter_type (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `LambdaBody`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_lambda_body < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Start < & 'a LambdaBody < 'a > , & 'a Layout < 'a > > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: START_LAMBDA_BODY , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_start_lambda_body (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
/// Parses `input` starting from `Layout`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
 pub fn parse_layout < 'a > (input : & Input , tree_arena : & 'a Arena) -> std :: result :: Result < ParseSuccess < & 'a Layout < 'a > > , ParseError > { let vec_arena = Arena :: new () ; let mut parser = JavaParser :: new (input , grammar_data :: LAYOUT , & vec_arena) ; match parser . run () { ParseResult :: Success (success) => { let parse_duration = success . duration ; let tree_start = iguana_runtime :: Instant :: now () ; let parse_tree_builder = JavaParseTreeBuilder :: new (tree_arena) ; let tree = parse_tree :: create_parse_tree_layout (success . sppf_node_id , & parser , & parse_tree_builder) ; let tree_construction_duration = tree_start . elapsed () ; let ambiguity_node_added = parser . ambiguity_node_added () ; Ok (ParseSuccess { tree , parse_duration , tree_construction_duration , ambiguity_node_added }) } ParseResult :: Failure (error) => { let (line , column , message) = parser . format_error (& error) ; let len = parser . error_span_len (error . input_index) ; Err (ParseError { line , column , len , message }) } } }
